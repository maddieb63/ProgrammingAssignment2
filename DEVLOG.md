DEVLOG

3/29/26 Entry 1:
After creating all of my stack methods in ArrayStack I had to test them in my main. I started with push and pop to create a stack and then output the size and top to see if it was working correctly. At first it only output the first line and then stopped running the code. I was worried that the line of code tested in the output, the "top" method was the one not working, so I tried changing which input it put out but the code still would not run. I then realized it could be a problem with how I was creating my stack initially in the push method. I had added a condition to make it not add if the size was not right but this was throwing the entire method off so I got rid of the loop and just kept the line: data.push_back(item) and the stack out put correctly. My next issue will be making the proper if loop for the conditions of the push and also during the testing process I had some problems where I wanted to print the entire stack to see what it looked like, and I couldn't do that from main so I may have to make a print method, especially for when I have to print out the equations. 

3/30/26 Entry 2:
After lots of testing I have finally been able to get my isValidInfix method working. To make sure that I am using the correct ideas, I have not implemented checking for parentheses yet, only valid numbers or operands. I was going through the stack and popping off the values to check the next one. At the end I made it a temporary stack so that I didn't mess with the original stack, because during testing I had removed the "const" in the header since my pop was not working with a constant stack. No matter what I tried it was not returning false when I had an operator in the front. I tried moving around where I was comparing the current and previous operators. It turns out that it was not working because I never actually checked the last node, after popping everything, the last node value was held in the "previous" spot and was never checked so I added after the loop, a check to make sure that the last/first value was not an operator. The code now runs correctly for checking if the equations are infix, I can apply very similar logic to the postfix. 

3/30/26 Entry 3:
This session I am working on the isValidPostfix and the precedence. So far I have given a base outline to precedence based on simple PEMDAS logic, the only problem currently is they are not accounting for parentheses. I will go back to that later, but I am currently stumped because the method parameters is a string input, most likely from the contents of my stack, so I am not sure how to use that to look back at the stack and give higher precedence to operators that were found in the parentheses. Putting a pin in that one, and moving to isValidPostfix. It was easy to determine what made a normal infix expression valid or not based on my experiences and then figure out the code but I am more unfamiliar with postfix expressions. According to Stack Overflow, the best way to see if a postfix expression is valid is to count the operands (the numbers) and then decrease the count twice then increase it once when there is an operator. Ultimately, the count should end at positive 1 meaning that there is one less operator than operand. Always checking to see if it falls below 0 after every decrement. It wasn't working after I implemented the logic and I was confused and moving around where I put the counter, and trying to run the numbers through my head to see how it counted. I was checking both infix and postfix and the infix was reading it right but the postfix kept saying false when it should be true. I had used the base code structure from the isValidinfix and I realized that when determining validity, the counter is meant to catch whether or not there are two numbers next to each other, but it was giving the wrong answer because the counter assumed going from left to right of the expression, while my code from the other method and just in general, puts the expression in a stack where the top is the last element. I used a temporary stack to move items from my input stack into a new stack in the reverse order so that when I iterated through the stack, I was reading values the same direction I would be when looking at the expression. This solved my problem! Onto from infix to postfix. 

3/31/26 Entry 4:
This session I am working on the postfix evaluation. My first idea was to break the expression into two separate stacks for the operators and the numbers and put the result of the operations in the numbers and then move the operators to the operator stack. I first reversed the stack so that I can go through the expression from the beginning. I can go through and store numbers when they are found and then perform the following operation. I realized that I do not need to store the operators after they have been used so that stack is not needed. I check first to see if it is a number then I push it to my number stack. I was at first confused how to add the searching for an operand but I realized that no matter where in the expression you are, there will be two values before an operator so therefore two values in the number stack. Then when an operator is encountered I can pop the two values off the stack then put whatever operator is found on them. I am currently struggling with something in the testing. When I call the method, it is not outputting anything, and is not stopping the run of the code. I am going to make sure the return type is correct and adjust the order of the methods I call in main to see if that changes anything. Nothing in the order is the problem, I have found by putting a print statement in my while loop that prints the current value, that my loop is running infinitely and only outputting the top value. This is because looking back I never added a pop statement at the beginning of the while loop. Now the loop is going through all the values and printing them, but not giving a total value. After adding more print statements I have found that my error is when I am checking the operands, I was using the variable I made during my two stack method "operand" which hold no actual value anymore so there were no math actions done on the values. AFter changing this variable name and a couple other small things the eval method works on short postfix expressions I will continue more testing with more complex expressions to see if there are any other edge cases.
